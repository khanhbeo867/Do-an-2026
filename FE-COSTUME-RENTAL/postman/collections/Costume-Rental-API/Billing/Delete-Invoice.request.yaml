$kind: http-request
name: Delete Invoice
description: |-
  Xóa hóa đơn theo `id`.

  - `permanantly=false` (mặc định): xóa mềm
  - `permanantly=true`: xóa cứng.
method: DELETE
url: '{{baseUrl}}/invoices/:id'
order: 6600
pathVariables:
  id: '1'
params:
  - key: permanantly
    value: 'false'
    disabled: true
